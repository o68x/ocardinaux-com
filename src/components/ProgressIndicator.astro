<div id="progress"></div>

<style>
  html {
    scroll-timeline: --page-scroll block;
  }

  @keyframes grow-progress {
    from {
      transform: scaleX(0);
    }

    to {
      transform: scaleX(1);
    }
  }

  #progress {
    display: none;
  }

  @supports (animation-timeline: scroll()) {
    #progress {
      display: block;
      position: fixed;
      left: 0;
      top: 0;
      width: 100%;
      height: 4px;
      background: var(--color-light-fern);
      z-index: 1000;
      transform-origin: 0 50%;
      animation: grow-progress auto linear;
      animation-timeline: --page-scroll;
    }
  }
</style>
