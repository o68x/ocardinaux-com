---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={[className, "tldr"]}>
  <slot />
</div>

<style>
  .tldr {
    padding: var(--s-1);
    font-size: var(--s-1);
  }
</style>
